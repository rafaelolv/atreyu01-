import React from 'react';

import style from '../style/Footer.module.css';

const Footer = () => {
    return(
        <footer className={style.footer}>
            <h2>Isso é um rodapé!</h2>
        </footer>
    );
};

export default Footer;